# agentic-day2-routing
Design a **minimal but production-minded LangGraph workflow** that:  - Tracks conversation state in a typed `SupportState` - Routes users to different paths based on **user tier** (`vip` vs `standard`) - Makes the routing logic **explicit, testable, and auditable**
